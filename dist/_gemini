#compdef gemini
# gemini zsh completion — generated by tab-please from gemini 0.45.0.
# Do not edit by hand: regenerate with `bun generator/build.ts gemini`.
# Enrichment (value actions, dynamic helpers) lives in tools/gemini/.

_gemini() {
  local curcontext="$curcontext" state line
  _arguments -C \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(-m --model)'{-m,--model}'[Model]::string:' \
    '(-p --prompt)'{-p,--prompt}'[Run in non-interactive (headless) mode with the given prompt. Appended to input on stdin (if any).]::string:' \
    '(-i --prompt-interactive)'{-i,--prompt-interactive}'[Execute the provided prompt and continue in interactive mode]::string:' \
    '(--skip-trust)--skip-trust[Trust the current workspace for this session.]' \
    '(-w --worktree)'{-w,--worktree}'[Start Gemini in a new git worktree. If no name is provided, one is generated automatically.]::string:' \
    '(-s --sandbox)'{-s,--sandbox}'[Run in sandbox?]' \
    '(-y --yolo)'{-y,--yolo}'[Automatically accept all actions (aka YOLO mode, see https://www.youtube.com/watch?v=xvFZjo5PgG0 for more details)?]' \
    '(--approval-mode)--approval-mode[Set the approval mode: default (prompt for approval), auto_edit (auto-approve edit tools), yolo (auto-approve all tools), plan (read-only mode)]::choice:(default auto_edit yolo plan)' \
    '*--policy[Additional policy files or directories to load (comma-separated or multiple --policy)]::value:' \
    '*--admin-policy[Additional admin policy files or directories to load (comma-separated or multiple --admin-policy)]::value:' \
    '(--acp)--acp[Starts the agent in ACP mode]' \
    '(--experimental-acp)--experimental-acp[Starts the agent in ACP mode (deprecated, use --acp instead)]' \
    '*--allowed-mcp-server-names[Allowed MCP server names]::value:' \
    '*--allowed-tools[DEPRECATED: Use Policy Engine instead See https://geminicli.com/docs/core/policy-engine Tools that are allowed to run without confirmation]::value:' \
    '*'{-e,--extensions}'[A list of extensions to use. If not provided, all extensions are used.]::value:' \
    '(-l --list-extensions)'{-l,--list-extensions}'[List all available extensions and exit.]' \
    '(-r --resume)'{-r,--resume}'[Resume a previous session. Use "latest" for most recent or index number (e.g. --resume 5)]::string:' \
    '(--session-file)--session-file[Load a session from a JSON file]::string:' \
    '(--session-id)--session-id[Start a new session with a manually provided UUID.]::string:' \
    '(--list-sessions)--list-sessions[List available sessions for the current project and exit.]' \
    '(--delete-session)--delete-session[Delete a session by index number (use --list-sessions to see available sessions).]::string:' \
    '*--include-directories[Additional directories to include in the workspace (comma-separated or multiple --include-directories)]::value:' \
    '(--screen-reader)--screen-reader[Enable screen reader mode for accessibility.]' \
    '(-o --output-format)'{-o,--output-format}'[The format of the CLI output.]::choice:(text json stream-json)' \
    '(--raw-output)--raw-output[Disable sanitization of model output (e.g. allow ANSI escape sequences). WARNING: This can be a security risk if the model output is untrusted.]' \
    '(--accept-raw-output-risk)--accept-raw-output-risk[Suppress the security warning when using --raw-output.]' \
    '(-h --help)'{-h,--help}'[Display help for command]' \
    '1: :_gemini_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $line[1] in
        mcp) _gemini_mcp ;;
        extensions|extension) _gemini_extensions ;;
        skills|skill) _gemini_skills ;;
        hooks|hook) _gemini_hooks ;;
        gemma) _gemini_gemma ;;
      esac
      ;;
  esac
}

_gemini_commands() {
  local -a _c=(
    'mcp:Manage MCP servers'
    'extensions:Manage Gemini CLI extensions.'
    'skills:Manage agent skills.'
    'hooks:Manage Gemini CLI hooks.'
    'gemma:Manage local Gemma model routing'
    'help:Display help for command'
  )
  _describe -t commands 'gemini command' _c
}

_gemini_mcp() {
  local curcontext="$curcontext" state line
  _arguments -C \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(-h --help)'{-h,--help}'[Display help for command]' \
    '1: :_gemini_mcp_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $line[1] in
        add) _gemini_mcp_add ;;
        remove) _gemini_mcp_remove ;;
        list) _gemini_mcp_list ;;
        enable) _gemini_mcp_enable ;;
        disable) _gemini_mcp_disable ;;
      esac
      ;;
  esac
}

_gemini_mcp_commands() {
  local -a _c=(
    'add:Add a server'
    'remove:Remove a server'
    'list:List all configured MCP servers'
    'enable:Enable an MCP server'
    'disable:Disable an MCP server'
    'help:Display help for command'
  )
  _describe -t commands 'mcp command' _c
}

_gemini_mcp_add() {
  _arguments \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(-s --scope)'{-s,--scope}'[Configuration scope (user or project)]::choice:(user project)' \
    '(-t --transport --type)'{-t,--transport,--type}'[Transport type (stdio, sse, http)]::choice:(stdio sse http)' \
    '*'{-e,--env}'[Set environment variables (e.g. -e KEY=value)]::value:' \
    '*'{-H,--header}'[Set HTTP headers for SSE and HTTP transports (e.g. -H "X-Api-Key: abc123" -H "Authorization: Bearer abc123")]::value:' \
    '(--timeout)--timeout[Set connection timeout in milliseconds]::number:' \
    '(--trust)--trust[Trust the server (bypass all tool call confirmation prompts)]' \
    '(--description)--description[Set the description for the server]::string:' \
    '*--include-tools[A comma-separated list of tools to include]::value:' \
    '*--exclude-tools[A comma-separated list of tools to exclude]::value:' \
    '(-h --help)'{-h,--help}'[Display help for command]' \
    '1:name:' \
    '2:commandOrUrl:' \
    '3:args:'
}

_gemini_mcp_remove() {
  _arguments \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(-s --scope)'{-s,--scope}'[Configuration scope (user or project)]::choice:(user project)' \
    '(-h --help)'{-h,--help}'[Display help for command]' \
    '1:name:'
}

_gemini_mcp_list() {
  _arguments \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(-h --help)'{-h,--help}'[Display help for command]'
}

_gemini_mcp_enable() {
  _arguments \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(--session)--session[Clear session-only disable]' \
    '(-h --help)'{-h,--help}'[Display help for command]' \
    '1:name:'
}

_gemini_mcp_disable() {
  _arguments \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(--session)--session[Disable for current session only]' \
    '(-h --help)'{-h,--help}'[Display help for command]' \
    '1:name:'
}

_gemini_extensions() {
  local curcontext="$curcontext" state line
  _arguments -C \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(-h --help)'{-h,--help}'[Display help for command]' \
    '1: :_gemini_extensions_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $line[1] in
        install) _gemini_extensions_install ;;
        uninstall) _gemini_extensions_uninstall ;;
        list) _gemini_extensions_list ;;
        update) _gemini_extensions_update ;;
        disable) _gemini_extensions_disable ;;
        enable) _gemini_extensions_enable ;;
        link) _gemini_extensions_link ;;
        new) _gemini_extensions_new ;;
        validate) _gemini_extensions_validate ;;
        config) _gemini_extensions_config ;;
      esac
      ;;
  esac
}

_gemini_extensions_commands() {
  local -a _c=(
    'install:Installs an extension from a git repository URL or a local path.'
    'uninstall:Uninstalls one or more extensions.'
    'list:Lists installed extensions.'
    'update:Updates all extensions or a named extension to the latest version.'
    'disable:Disables an extension.'
    'enable:Enables an extension.'
    'link:Links an extension from a local path. Updates made to the local path will always be reflected.'
    'new:Create a new extension from a boilerplate example.'
    'validate:Validates an extension from a local path.'
    'config:Configure extension settings.'
    'help:Display help for command'
  )
  _describe -t commands 'extensions command' _c
}

_gemini_extensions_install() {
  _arguments \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(--ref)--ref[The git ref to install from.]::string:' \
    '(--auto-update)--auto-update[Enable auto-update for this extension.]' \
    '(--pre-release)--pre-release[Enable pre-release versions for this extension.]' \
    '(--consent)--consent[Acknowledge the security risks of installing an extension and skip the confirmation prompt.]' \
    '(--skip-settings)--skip-settings[Skip the configuration on install process.]' \
    '(-h --help)'{-h,--help}'[Display help for command]' \
    '1:source:' \
    '2:--auto-update:' \
    '3:--pre-release:'
}

_gemini_extensions_uninstall() {
  _arguments \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(--all)--all[Uninstall all installed extensions.]' \
    '(-h --help)'{-h,--help}'[Display help for command]' \
    '1:names:'
}

_gemini_extensions_list() {
  _arguments \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(-o --output-format)'{-o,--output-format}'[The format of the CLI output.]::choice:(text json)' \
    '(-h --help)'{-h,--help}'[Display help for command]'
}

_gemini_extensions_update() {
  _arguments \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(--all)--all[Update all extensions.]' \
    '(-h --help)'{-h,--help}'[Display help for command]' \
    '1:<name:' \
    '2:--all:'
}

_gemini_extensions_disable() {
  _arguments \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(--scope)--scope[The scope to disable the extension in.]::string:' \
    '(-h --help)'{-h,--help}'[Display help for command]' \
    '1:--scope:' \
    '2:name:'
}

_gemini_extensions_enable() {
  _arguments \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(--scope)--scope[The scope to enable the extension in. If not set, will be enabled in all scopes.]::string:' \
    '(-h --help)'{-h,--help}'[Display help for command]' \
    '1:--scope:' \
    '2:name:'
}

_gemini_extensions_link() {
  _arguments \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(--consent)--consent[Acknowledge the security risks of installing an extension and skip the confirmation prompt.]' \
    '(-h --help)'{-h,--help}'[Display help for command]' \
    '1:path:'
}

_gemini_extensions_new() {
  _arguments \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(-h --help)'{-h,--help}'[Display help for command]' \
    '1:path:' \
    '2:template:'
}

_gemini_extensions_validate() {
  _arguments \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(-h --help)'{-h,--help}'[Display help for command]' \
    '1:path:'
}

_gemini_extensions_config() {
  _arguments \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(--scope)--scope[The scope to set the setting in.]::choice:(user workspace)' \
    '(-h --help)'{-h,--help}'[Display help for command]' \
    '1:name:' \
    '2:setting:'
}

_gemini_skills() {
  local curcontext="$curcontext" state line
  _arguments -C \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(-h --help)'{-h,--help}'[Display help for command]' \
    '1: :_gemini_skills_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $line[1] in
        list) _gemini_skills_list ;;
        enable) _gemini_skills_enable ;;
        disable) _gemini_skills_disable ;;
        install) _gemini_skills_install ;;
        link) _gemini_skills_link ;;
        uninstall) _gemini_skills_uninstall ;;
      esac
      ;;
  esac
}

_gemini_skills_commands() {
  local -a _c=(
    'list:Lists discovered agent skills.'
    'enable:Enables an agent skill.'
    'disable:Disables an agent skill.'
    'install:Installs an agent skill from a git repository URL or a local path.'
    'link:Links an agent skill from a local path. Updates to the source will be reflected immediately.'
    'uninstall:Uninstalls an agent skill by name.'
    'help:Display help for command'
  )
  _describe -t commands 'skills command' _c
}

_gemini_skills_list() {
  _arguments \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(--all)--all[Show all skills, including built-in ones.]' \
    '(-h --help)'{-h,--help}'[Display help for command]' \
    '1:--all:'
}

_gemini_skills_enable() {
  _arguments \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(-h --help)'{-h,--help}'[Display help for command]' \
    '1:name:'
}

_gemini_skills_disable() {
  _arguments \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(-s --scope)'{-s,--scope}'[The scope to disable the skill in (user or workspace).]::choice:(user workspace)' \
    '(-h --help)'{-h,--help}'[Display help for command]' \
    '1:name:' \
    '2:--scope:'
}

_gemini_skills_install() {
  _arguments \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(--scope)--scope[The scope to install the skill into. Defaults to "user" (global).]::choice:(user workspace)' \
    '(--path)--path[Sub-path within the repository to install from (only used for git repository sources).]::string:' \
    '(--consent)--consent[Acknowledge the security risks of installing a skill and skip the confirmation prompt.]' \
    '(-h --help)'{-h,--help}'[Display help for command]' \
    '1:source:' \
    '2:--scope:' \
    '3:--path:'
}

_gemini_skills_link() {
  _arguments \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(--scope)--scope[The scope to link the skill into. Defaults to "user" (global).]::choice:(user workspace)' \
    '(--consent)--consent[Acknowledge the security risks of linking a skill and skip the confirmation prompt.]' \
    '(-h --help)'{-h,--help}'[Display help for command]' \
    '1:path:'
}

_gemini_skills_uninstall() {
  _arguments \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(--scope)--scope[The scope to uninstall the skill from. Defaults to "user" (global).]::choice:(user workspace)' \
    '(-h --help)'{-h,--help}'[Display help for command]' \
    '1:name:' \
    '2:--scope:'
}

_gemini_hooks() {
  local curcontext="$curcontext" state line
  _arguments -C \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(-h --help)'{-h,--help}'[Display help for command]' \
    '1: :_gemini_hooks_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $line[1] in
        migrate) _gemini_hooks_migrate ;;
      esac
      ;;
  esac
}

_gemini_hooks_commands() {
  local -a _c=(
    'migrate:Migrate hooks from Claude Code to Gemini CLI'
    'help:Display help for command'
  )
  _describe -t commands 'hooks command' _c
}

_gemini_hooks_migrate() {
  _arguments \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(--from-claude)--from-claude[Migrate from Claude Code hooks]' \
    '(-h --help)'{-h,--help}'[Display help for command]'
}

_gemini_gemma() {
  local curcontext="$curcontext" state line
  _arguments -C \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(-h --help)'{-h,--help}'[Display help for command]' \
    '1: :_gemini_gemma_commands' \
    '*::arg:->args'
  case $state in
    args)
      case $line[1] in
        setup) _gemini_gemma_setup ;;
        start) _gemini_gemma_start ;;
        stop) _gemini_gemma_stop ;;
        status) _gemini_gemma_status ;;
        logs) _gemini_gemma_logs ;;
      esac
      ;;
  esac
}

_gemini_gemma_commands() {
  local -a _c=(
    'setup:Download and configure Gemma local model routing'
    'start:Start the LiteRT-LM server'
    'stop:Stop the LiteRT-LM server'
    'status:Check Gemma local model routing status'
    'logs:View LiteRT-LM server logs'
    'help:Display help for command'
  )
  _describe -t commands 'gemma command' _c
}

_gemini_gemma_setup() {
  _arguments \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(--port)--port[Port for the LiteRT server]::number:' \
    '(--skip-model)--skip-model[Skip model download (binary only)]' \
    '(--start)--start[Start the server after setup]' \
    '(--force)--force[Re-download binary and model even if already present]' \
    '(--consent)--consent[Skip interactive consent prompt (implies acceptance)]' \
    '(-h --help)'{-h,--help}'[Display help for command]'
}

_gemini_gemma_start() {
  _arguments \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(--port)--port[Port for the LiteRT server]::number:' \
    '(-h --help)'{-h,--help}'[Display help for command]'
}

_gemini_gemma_stop() {
  _arguments \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(--port)--port[Port where the LiteRT server is running]::number:' \
    '(-h --help)'{-h,--help}'[Display help for command]'
}

_gemini_gemma_status() {
  _arguments \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(--port)--port[Port to check for the LiteRT server]::number:' \
    '(-h --help)'{-h,--help}'[Display help for command]'
}

_gemini_gemma_logs() {
  _arguments \
    '(-d --debug)'{-d,--debug}'[Run in debug mode (open debug console with F12)]' \
    '(-n --lines)'{-n,--lines}'[Show the last N lines and exit (omit to follow live)]::number:' \
    '(-f --follow)'{-f,--follow}'[Follow log output (defaults to true when --lines is omitted)]' \
    '(-h --help)'{-h,--help}'[Display help for command]'
}

if [[ $funcstack[1] = _gemini ]]; then
  _gemini "$@"
elif (( $+functions[compdef] )); then
  compdef _gemini gemini
fi
